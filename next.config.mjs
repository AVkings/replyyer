/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone' removed for Vercel — Vercel handles its own tracing.
  // Use 'standalone' only for Docker/self-hosted. See https://nextjs.org/docs/app/api-reference/next-config-js/output
};

export default nextConfig;
